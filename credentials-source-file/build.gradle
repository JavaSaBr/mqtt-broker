plugins {
  id("java-library")
  id("configure-java")
  id("groovy")
}

description = "File-based Credentials Source Provider"

dependencies {
  api projects.base
  api projects.authenticationApi

  testImplementation projects.testSupport
  testFixturesApi projects.testSupport
}
