plugins {
  id("java-library")
  id("configure-java")
  id("groovy")
}

description = "Define most of domain classes"

dependencies {
  api projects.base

  testImplementation projects.testSupport
  testFixturesApi projects.testSupport
}
